{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "java -Dspring.profiles.active=prod -Dserver.port=${PORT:-8080} -jar target/learning-management-system-1.0.0.jar",
    "healthcheckPath": "/",
    "healthcheckTimeout": 1000,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 100
  }
}
